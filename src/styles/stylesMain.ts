import { StyleSheet } from "react-native";
import { PADDING_MAIN_CONTAINER } from "../environmentVariables/environmentVariables";

export const stylesMain = StyleSheet.create({
  containerMain: {
    paddingHorizontal: PADDING_MAIN_CONTAINER,
  }
})
